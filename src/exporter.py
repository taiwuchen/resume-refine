from docx import Document


def apply_changes(doc: Document, optimized: dict) -> Document:
    """Apply optimized content back to the document."""
    pass


def export_pdf(doc: Document, output_path: str) -> str:
    """Export document to PDF."""
    pass

